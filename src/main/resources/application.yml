spring:
  application:
     name: customerpurchase
  datasource:
    #url: jdbc:postgresql://db.oqvkcyidzajzuiypmimu.supabase.co:5432/postgres
    #url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
    #url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Navprabhat@10
    #driver-class-name: org.postgressql.Driver
  jpa:
    properties:
      hibernate:
         dialect: org.hibernate.dialect.PostgreSQLDialect

jpa:
  hibernate:
    ddl-auto: validate
  show-sql: true
  properties:
    hibernate:
      formate_sql: true

sql:
  init:
    mode: always

server:
  port: 8080

uiURL: http://localhost:5173


